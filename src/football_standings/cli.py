"""CLI layer: parses command-line arguments and invokes the application layer.

Scaffolding only (Milestone 0). No application logic lives here; this
module should only translate CLI input/output into calls on the
application layer.
"""
