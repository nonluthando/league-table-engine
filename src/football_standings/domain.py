"""Domain layer: core league standings model and rules.

Scaffolding only (Milestone 0). This module must remain free of any
dependency on the CLI, CSV, or filesystem so the domain stays usable
from any future interface (CLI, API, UI) without rewriting it.
"""
