"""Application layer: orchestrates the domain for a given use case.

Scaffolding only (Milestone 0). This layer depends on the domain and on
input/output abstractions, but never on CLI-specific concerns.
"""
